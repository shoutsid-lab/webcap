// webcap entry point.
// Placeholder bootstrap — full wiring (server + browser pool + payment poller)
// lands in Wave 3 (W3.1). Kept minimal so the scaffold typechecks and runs.
console.log('webcap bootstrap (scaffold)');
